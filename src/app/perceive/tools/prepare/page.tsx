import { PrepareWorkspace } from "@/components/perception-tools/PrepareWorkspace";

export const metadata = {
  title: "Prepare",
  description:
    "Perceptual image preparation — conversion, archival optimization, and standalone HTML export.",
};

export default function PreparePage() {
  return <PrepareWorkspace />;
}
